// Filename: src/model/enums/<EnumName>.ts  — one file per proto enum
// String values must match the @SerialName values in the Kotlin enum.
enum <EnumName> {
    <VALUE> = "<VALUE>",
    // ...
}

export default <EnumName>;
